# Decisions

Public-safe decision log for this repository. Limited to information safe
to retain in a potentially publishable repository — no internal
institutional documents, survey responses, account information, or private
coordination material.

| Date | Decision | Reason | Alternatives rejected | Decided by |
|---|---|---|---|---|
| 2026-09-14 | Adopt the `course-site` file tree (this run's specification) rather than an earlier, broader scaffold draft. | The earlier draft predates and conflicts with the approved plan's narrower scope for this stage. | Reconciling both trees; deferred until Stage 2. | External review |
| 2026-09-14 | Approve Stage 0 (Git init) and Stage 1 (vertical slice) only. Stage 2 (full deck, remaining modules, full resource library) is not approved. | Prove the architecture — Quarto website, Reveal.js deck, one interactive, all offline-capable — before committing to the full build. | Building the full course before any external review. | External review |
| 2026-09-14 | Test Quarto's native `embed-resources` + `self-contained-math` for offline math rendering before considering any vendored library. | Empirical test, not an assumption; avoids an unnecessary dependency if the native option works. | Vendoring KaTeX immediately; hand-authored equation SVGs. | External review |
| 2026-09-14 | No tokenization component in this slice; no `tiktoken` installation. When built, use illustrative subword splits with no token IDs — token IDs are arbitrary and not pedagogically necessary. | Avoids an unneeded install and avoids implying any specific tokenizer is Claude's. | Installing `tiktoken`; capturing IDs from a third-party tool. | External review |
| 2026-09-14 | No browser-automation tooling (Playwright, Chromium, Node, or any new plugin/Skill) in this slice. Use a documented human browser checklist instead. | Keeps the vertical slice dependency-free; automation is reconsidered only after this slice is reviewed. | Installing Playwright + Chromium now. | External review |
| 2026-09-14 | Six content-accuracy corrections (hallucination mechanism, "tokens to think," post-training scope, attention-head interpretability, autoregressive caching, tool-call framing) and the multinomial-logit/MLE precision rules are binding from this stage forward and recorded in `CLAUDE.md`. | Several draft claims overstated or misstated real mechanisms; corrected at external review before any content shipped. | Leaving the original phrasing and correcting later. | External review |
| 2026-09-14 | Checkpoint the Stage 1 prototype and its first-learner review in one commit (`c98c7dd`) before starting the redesign batch below. Everything after that commit stays uncommitted for review as one diff. | Preserves an honest, inspectable record of the prototype — including its known offline-math failure and pending browser checks — before it is superseded. | Skipping the checkpoint and only recording the redesign; squashing the whole batch into one commit. | External review |
| 2026-09-14 | Retarget the live talk to 35-40 minutes, with **no required live equation and no required live softmax demonstration**. This explicitly supersedes the Stage 1 design's two "never cut" live-equation slides and full live interactive demo. | The 45-minute design with two protected derivations left no slack once real content was built; the site is now the primary, standalone product, so the talk does not need to carry the full mathematics live. | Keeping the 45-minute, two-protected-equation design and cutting other content instead. | External review |
| 2026-09-14 | The existing Stage 1 three-slide deck (`slides/llms-to-agents.qmd`) is left untouched, as a historical prototype under the old design. It is not retrofitted to the new no-required-equations policy in this batch. | The new policy governs the not-yet-built full talk; editing the prototype to match would misrepresent it as a finished artifact under a design it never used. | Editing the three existing slides now. | External review |
| 2026-09-14 | Fixed a real error in `CLAUDE.md`'s CC3: an earlier version listed "product-level system instructions" as a post-training method. Corrected — training and post-training change parameters; system instructions, retrieved information, and tool results change inference-time context and are never post-training. | System instructions do not touch model weights; classifying them as post-training was a factual error, not a stylistic issue. | Leaving the error and adding a caveat elsewhere instead of fixing the rule itself. | External review |
| 2026-09-14 | Audited all eight existing claim rows (`S-01`...`S-08`) against the batch's mandatory technical corrections (post-training/context, backprop/optimizer, attention memory, R1/R1-Zero). No existing row required a wording change; none of the eight touch those topics. | Documents that the audit happened and found nothing, rather than leaving the question open. | Silently assuming no existing claims needed review. | External review |
| 2026-09-14 | FlashAttention is not mentioned anywhere in this batch's content. The new `why-transformers.qmd` lesson states standard dense attention's quadratic arithmetic cost (from Vaswani et al. Table 1) but makes no memory-specific or implementation-level claim, so the FlashAttention correction (distinguish arithmetic cost from implementation memory; no universal memory lower bound) has nothing to correct in this batch and is deferred to the future attention lesson. | Keeps the new lesson's claims narrow and fully sourced from material already verified for this batch, rather than introducing a new, unverified source (Dao et al.) to make a claim the lesson does not need. | Adding a brief FlashAttention mention now. | External review |
| 2026-09-14 | DeepSeek R1 / R1-Zero is not mentioned anywhere in this batch's content. Neither new lesson touches reinforcement learning or reasoning models, so the correction (distinguish R1-Zero from R1; avoid unsupported "first" claims) has nothing to apply to and is deferred to the future post-training/agents lessons. | Confirms the correction was checked against actual batch content, not assumed irrelevant. | Adding an RL/reasoning-models aside to force the correction in. | External review |
| 2026-09-14 | Verified the two new Vaswani et al. locators (Sections 1 and 4, including the Table 1 figures for sequential operations, maximum path length, and complexity per layer) against the live paper before citing them, since these are new claims not previously cited anywhere in this repository. | Distinguishes a genuinely new source-verification need from the already-tracked, unresolved gap (G1) covering reused S1-S4/S10 timestamps. | Citing the new locators without checking them, on the strength of S10 already being an approved source for other sections. | External review |
| 2026-09-14 | Second first-learner review: the course is no longer written for economists (or any named profession) as its public audience. Replaced the PhD-economist framing everywhere in public-facing pages and `CLAUDE.md` with a general reader model (comfortable with basic algebra/probability, may have seen calculus/stats before but not recently, little or no ML vocabulary). The multinomial-logit bridge in `learn/softmax-sampling.qmd` is kept, but reframed as optional and conditional ("for readers who already know X"), never as a description of who the course is for. | The audience-specific framing assumed background this course can no longer assume; the underlying discrete-choice mathematics is still useful to readers who have it, so it stays as an explicitly optional bridge rather than being deleted. | Deleting the multinomial-logit section entirely; keeping the PhD-economist framing and only softening isolated sentences. | External review |
| 2026-09-14 | Added three new binding writing rules to `CLAUDE.md`: brief reminders when a concept returns (never "as you know"/"recall"); aggressive jargon reduction (ordinary language before a technical term, at first meaningful use, explaining why the concept exists); and a "Think of it this way" intuition callout after genuinely difficult concepts. Updated `.claude/skills/reader-first-course-editor/SKILL.md` to enforce all three in a dedicated editing pass, and re-ran that pass on all three existing lessons. | The course's own reader model requires this; a page that is technically correct but unreadable by the intended reader is not finished, per the new explanation standard in `CLAUDE.md`. | Leaving jargon-reduction as an unstated expectation instead of a checked editorial rule. | External review |
| 2026-09-14 | Added binding slide-design rules to `CLAUDE.md` and `docs/live-talk-storyboard.md`: one idea per slide, minimal on-slide prose, no slide justified only by a website section's existence, titles that carry the takeaway, and a conservative 1280x720 density budget. Explicitly declared the Stage 1 slides (and any other early-drafted slides) style experiments, not approved content — their technical content is not required to survive into the final deck. The live deck's required story is narrowed to training data -> neural network -> transformer -> next-token prediction -> training -> assistant -> tools -> agent; everything else is explanatory support that belongs on the website. | Prevents the same zero-slack, text-heavy design that produced the superseded 45-minute route from recurring when the full deck is eventually built. | Treating the existing storyboard's 21-segment table as slide-ready content; leaving slide density unconstrained until the full-deck build. | External review |
| 2026-09-15 | Built the two text-to-numbers pages: `learn/how-created.qmd` (main path: raw text → tokens → token IDs → embedding vectors → sequence presented to the model → next-token targets → handoff to training) and `learn/tokens-context.qmd` (optional deep dive: why subword units, a BPE-style merge example, spaces/punctuation, spelling consequences, the embedding table and the ID/embedding/contextual-representation distinction, the context window and its practical consequences). All tokenizations shown are labeled illustrative; the only real tokenizer figures are GPT-2's, verified against the paper. Nine claim rows added (S-27–S-35) from seven newly verified sources; product-specific context-window facts are access-dated and labeled per PR9. A small checker (`scripts/check_bpe_example.py`) recomputes the BPE merge counts and the context-budget arithmetic. Not committed, pending external learner review. | The site architecture placed tokens/context next in sequence; the main-path page had to exist before the neural-network lesson could be read as the second step of a deliberate sequence. Real tokenizer output was not available offline without installing a package, which the batch forbade. | Installing `tiktoken` to show real splits; writing one combined page; deferring the context-window material to the agents lesson. | External learner review |
| 2026-09-15 | Built the two Batch 2 pages: `learn/transformers-attention.qmd` (optional deep dive: why information must move, one head as mechanism before names, the scaled dot-product equations per position, causal masking, a 3-position numerical example, multi-head attention, the block hierarchy with residual/normalization ordering left architecture-specific, what attention weights do not mean, the quadratic cost link) and `learn/training.qmd` (main path: the autoregressive factorization, parallel evaluation under the causal mask versus sequential generation, logits → softmax → negative log-likelihood → maximum likelihood with PR2, the optimization loop with backprop/optimizer kept distinct, a two-model likelihood example, what pretraining learns, prediction versus verification, pretraining/post-training/context, and KV reuse as an inference optimization). Ten claim rows added (S-36–S-45) from Vaswani, GPT-2, Bengio, and Shazeer; two checkers added. Not committed, pending external learner review. | The site architecture named these as the next two modules; the attention page is the sequel `why-transformers.qmd` explicitly stops before, and the training page is the rescoped module that assumes the single-unit backpropagation lesson rather than repeating it. | One combined transformer page; deriving backpropagation again on the training page; teaching post-training methods on the training page. | External learner review |
| 2026-09-15 | Built the two Batch 3 pages: `learn/posttraining.qmd` (main path: the gap pretraining leaves; post-training defined by the parameter-update invariant and separated from system prompts, retrieval, tools, and files; supervised instruction tuning; why demonstrations are not enough; preference-based methods with the reward-model/RL route and the DPO route kept distinct; model-generated feedback via Constitutional AI; reinforcement learning against checkable rewards with DeepSeek-R1-Zero and R1 kept apart and CC2 applied; post-training does not replace pretraining; model versus assistant) and `learn/agents.qmd` (main path: what a bare model call cannot do; the tool-call round trip; a sustained debug-and-fix example labeled by step type; workflow versus agent per Anthropic's 2024 article; the agent loop as a shared include; ingredients; parameters/context/external state; skills as context; verification per PR6; permissions as system-level controls; assistant to coding agent with Claude Code as the access-dated example; failure modes). Eleven claim rows added (S-46–S-56); all product facts access-dated 2026-09-15. No numerical examples, so no new checkers. Not committed, pending external learner review. | These are the last two conceptual modules in the site architecture and the bridge the practical pages need; the storyboard's segments 14–20 map onto them. | Folding post-training into the training page; describing agents as an architecture; using RLHF as the name for all post-training. | External learner review |
